[package]
name = "types"
version = "0.1.0"
edition = "2021"
description = "Shared types for rootsmith"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
postcard = { version = "1.1.3", features = ["alloc"] }
