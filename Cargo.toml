[workspace]
resolver = "2"
members = [
    "rootsmith",
    "rootsmith-wasm-host",
    "rootsmith-plugin-sdk",
    "examples/plugin",
]

# Exclude no_std crates from default build (they require wasm32 target)
default-members = [
    "rootsmith",
    "rootsmith-wasm-host",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
