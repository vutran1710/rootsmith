[package]
name = "example_parser_plugin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rootsmith-plugin-sdk = { path = "../../rootsmith-plugin-sdk" }
serde = { version = "1", default-features = false, features = ["derive"] }
