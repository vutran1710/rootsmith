syntax = "proto3";

package rootsmith;

message Record {
  bytes namespace = 1;
  bytes key = 2;
  bytes value = 3;
  uint64 timestamp = 4;
  optional string metadata = 5;
}

message Commitment {
  repeated bytes namespaces = 1;
  bytes root = 2;
  uint64 committed_at = 3;
}
